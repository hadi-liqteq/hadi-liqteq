# Fingerprint registry

One row per build. A new build must differ from every existing row on at least
4 of the 6 dimensions (grammar, nav, hero, act-sequence, close, signature move).

| # | Build | World / port | Grammar | Nav treatment | Hero device | Act-sequence shape | Close pattern | Signature move |
|---|-------|--------------|---------|---------------|-------------|--------------------|---------------|----------------|
| 1 | Coarts Lighting | CSS-rendered light on a warm cream ground (no photography, no video); light port | Gallery / catalog | Fixed slim bar: wordmark + jump index of rooms + Enquiry (with count badge); mobile sheet | Pinned welcome scene, object one already lit and labelled, greet cue (no separate title stage) | pin → pan → pin(bloom) → flow(specs) → flow/reveal(grid) → pin(close); 6 acts, ~11.5vh pinned + 2 flow | Pinned inquiry plate, holds; eyebrow + one line + Enquire, socials, footer inside the stage | The dimmer: a fixed colour-temperature + brightness control that re-lights the entire page (ground, glows, accent, Kelvin) from two CSS custom properties |
| 2 | Liqteq (Liquid Technologies) | All-type on a dark deep-violet ground, drift within the violet family (no photography, no video); dark port | Typographic poster | Minimal fixed bar: droplet mark + wordmark, one underlined "Start a project"; wordmark also at composition scale in the hero | Extreme-scale liquid word ("Liquid") with a greet cue; kicker above, thesis below | pin(scale/liquid) → pin(kinetic claim) → pin(reveal caps) → pin(reveal peak) → flow(proof+count) → pin(close); 6 acts, ~13.5vh | Inverted quiet: smallest type, "Start a project" as an underlined link, real contact + footer inside the stage, holds | Liquid type: the hero word is a variable font whose glyphs flow toward the pointer and undulate with scroll, so the wordmark behaves like a fluid |

Shared columns to avoid next time: light-on-cream (row 1) and dark-violet all-type
(row 2) worlds; gallery (1) and typographic-poster (2) grammars; the whole-page
dimmer (1) and the liquid variable-type (2) signatures. Rows 1 and 2 differ on
all six dimensions.
